spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/MS_DB?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 20010419 # 학생들에게 각자 설정한 비밀번호를 입력하라고 안내

  jpa:
    hibernate:
      ddl-auto: update # 엔티티 변경사항을 DB 테이블에 자동 반영
    show-sql: true # 실행되는 쿼리를 콘솔에 출력
    properties:
      hibernate:
        format_sql: true # 콘솔에 출력되는 쿼리를 예쁘게 정렬